spring:
  application:
    name: downtime

  datasource:
    url: jdbc:postgresql://localhost:5432/downtime_app
    username: noman
    password: Getsuga

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
