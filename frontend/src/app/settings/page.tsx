import { resourceService } from "@/services/resourceService";
export const dynamic = "force-dynamic";
export default async function SettingsPage(){const result=await Promise.allSettled([resourceService.departments(),resourceService.productionLines(),resourceService.downtimeReasons()]);const departments=result[0].status==="fulfilled"?result[0].value:[];const lines=result[1].status==="fulfilled"?result[1].value:[];const reasons=result[2].status==="fulfilled"?result[2].value:[];return <div className="mx-auto max-w-7xl"><p className="text-xs font-bold uppercase tracking-[.2em] text-cyan-700">Reference data</p><h1 className="mt-2 text-3xl font-bold">Plant setup</h1><p className="mt-2 text-sm text-slate-500">A connected view of organizational and downtime classification data.</p><div className="mt-8 grid gap-6 xl:grid-cols-3"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold">Departments <span className="float-right rounded-full bg-slate-100 px-2 py-1 text-xs">{departments.length}</span></h2><div className="mt-4 divide-y divide-slate-100">{departments.map(d=><div key={d.id} className="py-3"><p className="text-sm font-semibold">{d.name}</p><p className="mt-1 text-xs text-slate-500">{d.location||d.description||"No details"}</p></div>)}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold">Production lines <span className="float-right rounded-full bg-slate-100 px-2 py-1 text-xs">{lines.length}</span></h2><div className="mt-4 divide-y divide-slate-100">{lines.map(l=><div key={l.id} className="py-3"><p className="text-sm font-semibold">{l.name}</p><p className="mt-1 text-xs text-slate-500">{l.department.name} · {l.location||"No location"}</p></div>)}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold">Downtime reasons <span className="float-right rounded-full bg-slate-100 px-2 py-1 text-xs">{reasons.length}</span></h2><div className="mt-4 divide-y divide-slate-100">{reasons.map(r=><div key={r.id} className="flex items-center justify-between py-3"><div><p className="text-sm font-semibold">{r.name}</p><p className="mt-1 text-xs text-slate-500">{r.category||"Uncategorized"}</p></div><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${r.planned?"bg-cyan-50 text-cyan-700":"bg-amber-50 text-amber-700"}`}>{r.planned?"PLANNED":"UNPLANNED"}</span></div>)}</div></section></div></div>}
