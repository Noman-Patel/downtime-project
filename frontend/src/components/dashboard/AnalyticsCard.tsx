export function AnalyticsCard({ title, rows, valueKey }: { title: string; rows: { label: string; value: number }[]; valueKey: string }) {
  const max = Math.max(...rows.map((row) => row.value), 1);
  return <section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="mb-6 flex items-center justify-between"><div><h2 className="font-semibold text-slate-900">{title}</h2><p className="mt-1 text-xs text-slate-500">Event distribution across your plant</p></div><span className="rounded-lg bg-slate-100 px-2 py-1 text-xs text-slate-500">Live</span></div>{rows.length ? <div className="space-y-5">{rows.slice(0, 6).map((row, index) => <div key={`${row.label}-${index}`}><div className="mb-2 flex justify-between text-sm"><span className="truncate pr-3 font-medium text-slate-700">{row.label || "Uncategorized"}</span><span className="text-slate-500">{row.value} {valueKey}</span></div><div className="h-2.5 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-cyan-400" style={{ width: `${Math.max((row.value / max) * 100, 4)}%` }} /></div></div>)}</div> : <div className="grid h-48 place-items-center rounded-xl border border-dashed border-slate-200 text-sm text-slate-400">No downtime data yet</div>}</section>;
}
